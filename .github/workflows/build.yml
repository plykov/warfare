name: Build and verify

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-build:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Install Godot 4.4.1
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.4.1
          use-dotnet: false
          include-templates: true

      - name: Verify engine version
        shell: pwsh
        run: godot --version

      - name: Run project checks
        shell: pwsh
        run: powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/verify.ps1

      - name: Export Windows release
        shell: pwsh
        run: powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/build_windows.ps1

      - name: Smoke-test exported executable
        shell: pwsh
        run: powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/smoke_windows.ps1

      - name: Add player instructions
        shell: pwsh
        run: Copy-Item README.md build/README.md

      - name: Upload Windows build
        uses: actions/upload-artifact@v7
        with:
          name: Garden-Reclaimed-Windows
          path: |
            build/Garden-Reclaimed.exe
            build/README.md
          if-no-files-found: error
          retention-days: 14

  linux-build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Install Godot 4.4.1
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.4.1
          use-dotnet: false
          include-templates: true

      - name: Verify engine version
        run: godot --version

      - name: Run project checks
        run: ./scripts/verify.sh godot

      - name: Export Linux release
        run: ./scripts/build_export.sh "Linux Desktop" build/Garden-Reclaimed.x86_64 godot

      - name: Smoke-test exported executable
        run: ./scripts/smoke_export.sh build/Garden-Reclaimed.x86_64

      - name: Add player instructions
        run: cp README.md build/README.md

      - name: Upload Linux build
        uses: actions/upload-artifact@v7
        with:
          name: Garden-Reclaimed-Linux
          path: |
            build/Garden-Reclaimed.x86_64
            build/README.md
          if-no-files-found: error
          retention-days: 14
